services:
  rng-api:
    ports:
      - "8289:80"

  rng-web:
    ports:
      - "8290:80"

secrets:
  dotnet-logging:
    file: ./config/test/logging.json
  rng-api:
    file: ./config/test/api/override.json
  rng-web:
    file: ./config/test/web/override.json
